def solution(arr):
    res = []

    if len(arr) <= 1:
        arr[0] = -1
        res = arr
    
    for i in arr: 
        if i > min(arr):
            res.append(i)
 
    return res

# Below is a test to see code.
    arr = [4,3,2,1]
    print( "result:".format(solution(arr)) )
